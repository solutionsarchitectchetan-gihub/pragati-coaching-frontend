services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: http://localhost:8080/api
    container_name: pragati-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
