services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: https://api.pragatitech.in/api
    container_name: pragati-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
